<Window x:Class="IFC_Parameter_Automation.AddParameters"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IFC_Parameter_Automation"
             mc:Ignorable="d" Height="600" Width="300"
             d:DesignHeight="170" d:DesignWidth="300" Title="Add Parameters" Background="LightGray" WindowStartupLocation="CenterScreen">
    <Border Background="#FFEDEDED" Margin="10" BorderBrush="#FFD0D0D0" 
            BorderThickness="1" CornerRadius="10" >
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                 </Grid.RowDefinitions>
                <TextBlock x:Name="Select_Element" Text="Select Element"/>
                <Button x:Name="Picking_Object" Content="Pick Object" Grid.Column="0" Width="120" Margin="0 20" Click="Pick_Object" ToolTip="Select a door or a wall" Cursor="Hand"/>
                <TextBlock x:Name="Select_Category" Text="Select Category" Grid.Column="1"/>
                <DataGrid x:Name="Category_List" Grid.Column="1" Width="120" Margin="0 20"/>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <ListView ItemsSource="{Binding Properties}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <CheckBox IsChecked="{Binding IsSelected}" Margin="0,0,10,0"/>
                                <TextBlock Text="{Binding Code}" FontWeight="Bold" Width="200"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button x:Name="Cancel" Content="Cancel" Grid.Column="1" Width="90" Margin="0 20" Click="Cancel_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
